<%- include("header") -%>
    <table class="table table-hover">
        <thead>
          <tr>
            <th scope="col">#</th>
            <th scope="col">UserName</th>
            <th scope="col">Email</th>
            <th scope="col">Mobile No.</th>
            <th scope="col">Department Name</th>
            <th scope="col">Salary</th>
          </tr>
        </thead>

   <% i=0 %>     
<%  entry.forEach(function(entry){  i++ %>
        <tbody>
          <tr>
            <th scope="row">
              <%= i %>
            </th>
            <td><%= entry.username %></td>
            <td><%= entry.email %></td>
            <td><%= entry.phone %></td>
            <td align="justified"><%= entry.dept %></td>
            <td><%= entry.salary %></td>
            <td>
                <form action="/delentry" method="post">
                    <input type="hidden" value="<%= entry._id %>" name="lid">
                    <button type="submit" >💣</button>
                    <a href="/editentry/<%= entry._id %>" class="btn btn-danger btn-sm">Edit</a>
                </form></td>
                
                 
              
          </tr<>
        </tbody>

        <% }); %>
      </table>



<%- include("footer") -%>